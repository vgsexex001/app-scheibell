# =================================
# App Scheibell - Backend Configuration
# =================================

# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
# For local development:
DATABASE_URL="postgresql://postgres:password@localhost:5432/app_scheibell?schema=public"

# For Google Cloud SQL (production):
# DATABASE_URL="postgresql://USER:PASSWORD@/DATABASE?host=/cloudsql/PROJECT:REGION:INSTANCE"

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=24h

# CORS (comma-separated origins)
# Development:
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Production (update with your actual domains):
# CORS_ORIGINS=https://your-app.web.app,https://your-domain.com

# =================================
# Google Cloud Configuration (Production)
# =================================

# Cloud SQL Instance Connection Name
# Format: PROJECT_ID:REGION:INSTANCE_NAME
# CLOUD_SQL_CONNECTION_NAME=your-project:us-central1:your-instance

# =================================
# Optional: Additional Services
# =================================

# Firebase (if using for authentication or push notifications)
# FIREBASE_PROJECT_ID=your-firebase-project-id
# FIREBASE_PRIVATE_KEY="your-private-key"
# FIREBASE_CLIENT_EMAIL=your-client-email@your-project.iam.gserviceaccount.com

# Logging (optional)
# LOG_LEVEL=debug

# Rate Limiting
# THROTTLE_TTL=60
# THROTTLE_LIMIT=100
